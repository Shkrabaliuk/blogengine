# 🚀 Швидкий старт

## Покрокова інструкція

### Крок 1: Завантаження архіву
Розпакуйте `simple-blog-engine.tar.gz` на хостинг:
```bash
tar -xzf simple-blog-engine.tar.gz
```

### Крок 2: Створіть необхідні папки
```bash
mkdir data uploads
chmod 777 data uploads
```

### Крок 3: Згенеруйте хеш пароля

Створіть файл `generate_password.php` з таким кодом:
```php
<?php
$password = 'ваш_сильний_пароль_тут';
echo password_hash($password, PASSWORD_DEFAULT);
?>
```

Запустіть:
```bash
php generate_password.php
```

Скопіюйте результат.

### Крок 4: Налаштуйте пароль

Відредагуйте `system/core.php`, знайдіть рядок:
```php
$admin_pass = password_hash('ваш_сильний_пароль_тут', PASSWORD_DEFAULT);
```

Замініть на:
```php
$admin_pass = 'ваш_згенерований_хеш';
```

### Крок 5: Готово!

Відкрийте в браузері:
- Головна: `https://ваш-домен.com/`
- Вхід: `https://ваш-домен.com/login.php`

## Структура файлів після розпакування

```
/
├── system/
│   ├── core.php        ← Тут встановлюється пароль
│   └── upload.php
├── data/               ← Створіть цю папку (777)
├── uploads/            ← Створіть цю папку (777)
├── .htaccess
├── index.php
├── style.css
├── login.php
├── README.md           ← Детальна документація
└── SECURITY.md         ← Інформація про безпеку
```

## Перший пост

1. Зайдіть на `/login.php`
2. Введіть пароль
3. На головній сторінці з'явиться редактор
4. Напишіть заголовок і текст
5. Додайте теги через кому (наприклад: `дизайн, код, веб`)
6. Натисніть "Опублікувати"

## Завантаження зображень

Просто перетягніть зображення в текстове поле - вони автоматично завантажаться!

## Що далі?

- 📖 Прочитайте повний `README.md`
- 🔒 Ознайомтесь з `SECURITY.md`
- 🎨 Кастомізуйте `style.css` під свій дизайн

---

**Потрібна допомога?** Перегляньте README.md або створіть issue.
